import React from "react";

const Box = ({ bg }) => {
  return (
    <div
      style={{
        background: bg,
      }}
      className="box"
    >
      Box
    </div>
  );
};

export default Box;
