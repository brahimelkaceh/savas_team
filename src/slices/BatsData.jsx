import { createSlice } from "@reduxjs/toolkit";

const BatsDataSlice = createSlice({
  name: "BatsData",
  initialState: {},
  reducers: {},
});
export const {} = BatsDataSlice.actions;
export default BatsDataSlice.reducer;
